renom.operation.log
===================

.. currentmodule:: renom.operation

.. autoclass:: log

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~log.__init__
      ~log.all
      ~log.any
      ~log.argmax
      ~log.argmin
      ~log.argpartition
      ~log.argsort
      ~log.as_ndarray
      ~log.astype
      ~log.backward
      ~log.byteswap
      ~log.calc_value
      ~log.choose
      ~log.clip
      ~log.compress
      ~log.conj
      ~log.conjugate
      ~log.copy
      ~log.copy_from
      ~log.cumprod
      ~log.cumsum
      ~log.detach_graph
      ~log.diagonal
      ~log.dot
      ~log.dump
      ~log.dumps
      ~log.fill
      ~log.flatten
      ~log.get_gpu
      ~log.getfield
      ~log.grad
      ~log.item
      ~log.itemset
      ~log.max
      ~log.mean
      ~log.min
      ~log.newbyteorder
      ~log.nonzero
      ~log.partition
      ~log.prod
      ~log.ptp
      ~log.put
      ~log.ravel
      ~log.release_gpu
      ~log.repeat
      ~log.reshape
      ~log.resize
      ~log.round
      ~log.searchsorted
      ~log.set_gpu
      ~log.set_hook
      ~log.set_model
      ~log.setfield
      ~log.setflags
      ~log.sort
      ~log.squeeze
      ~log.std
      ~log.sum
      ~log.swapaxes
      ~log.take
      ~log.to_cpu
      ~log.to_gpu
      ~log.tobytes
      ~log.tofile
      ~log.tolist
      ~log.tostring
      ~log.trace
      ~log.transpose
      ~log.var
      ~log.view
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~log.SHOWMARK
      ~log.T
      ~log.attrs
      ~log.auto_update
      ~log.base
      ~log.ctypes
      ~log.data
      ~log.device_id
      ~log.dtype
      ~log.flags
      ~log.flat
      ~log.imag
      ~log.itemsize
      ~log.nbytes
      ~log.ndim
      ~log.prevent_update
      ~log.real
      ~log.shape
      ~log.size
      ~log.strides
   
   